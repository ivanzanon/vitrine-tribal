import styled from 'styled-components';

export const CourseForm = styled.section`

    padding: 5rem;
    width: 80%;

    form {
        display: flex;
        flex-direction: column;

        label {
            margin-top: 2.5rem;
        }

        input {
            margin-top: 0.5rem;
        }
    }

    .input-name {
        width: 50%;
    }

`;
